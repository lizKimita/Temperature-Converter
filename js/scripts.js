var temperature = function(celsius, number2) {
 return celsius + number2;
};

var celsius = prompt ("Enter your temperature in degrees celsius here:");
var degrees = celsius * (1.8);
var number2 = 32;
var results = degrees + number2;
alert(results);
